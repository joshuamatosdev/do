### Memory routine (memory-discipline module)

When a durable fact is learned, use the `do-remember` skill: write one fact per file + a one-line `MEMORY.md` index pointer. Never inline temporary state into memory.
